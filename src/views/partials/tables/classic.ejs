<!-- @param {Array} locals.table -->
<table>
  <tbody>
    <% locals.table.forEach(row => { %>
      <tr>
        <th><%= row.name %></th>
        <td><%= row.value %></td>
      </tr>
    <% }); %>
  </tbody>
</table>